# ── 핵심 AI/STT ──────────────────────────────────
openai-whisper==20250625      # STT 엔진 (Whisper small 모델)
faster-whisper==1.2.1         # STT 서버용 (현재 미사용, 추후 전환 예정)
numpy==2.4.4                  # 오디오 배열 처리
scipy==1.17.1                 # 샘플레이트 변환 (48000→16000Hz)
torch==2.11.0                 # Whisper 의존성

# ── 오디오 입출력 ─────────────────────────────────
sounddevice==0.5.5            # 마이크 녹음 (wm8960 HAT, DEVICE_INDEX=0)
PyAudio==0.2.14               # PortAudio 바인딩

# ── TTS ──────────────────────────────────────────
edge-tts==7.2.8               # 한국어 TTS (ko-KR-SunHiNeural)

# ── STT 서버 ─────────────────────────────────────
Flask==3.1.3                  # STT HTTP 서버 (port 8081)

# ── HTTP 통신 ─────────────────────────────────────
requests==2.33.1              # llama-server API 호출, 백엔드 연동

# ── 언어 감지 ─────────────────────────────────────
langdetect==1.0.9             # 환각 방지 한국어 감지 (보조)

# ── 기타 ─────────────────────────────────────────
tiktoken==0.12.0              # Whisper 토크나이저 의존성
regex==2026.4.4               # Whisper 의존성
tqdm==4.67.3                  # 모델 다운로드 진행 표시
